import Foundation

// MARK: - Component Sizes

public enum WPSize {
    /// Standard avatar diameter (settings profile, etc.)
    public static let avatarMedium: CGFloat = 48
    /// FAB button diameter
    public static let fabButton: CGFloat = 56
    /// Bottom padding to clear tab bar when FAB is overlaid
    public static let fabBottomOffset: CGFloat = 80
}
