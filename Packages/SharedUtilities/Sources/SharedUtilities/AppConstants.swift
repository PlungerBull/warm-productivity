import Foundation

public enum AppConstants {
    /// Shared App Group identifier used for cross-app data sharing (SwiftData, UserDefaults, Keychain).
    public static let appGroupIdentifier = "group.com.warmproductivity.shared"
}
